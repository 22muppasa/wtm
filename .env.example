EXPO_PUBLIC_DEMO_MODE=true
# Optional future adapter configuration. The current app always uses local repositories.
# No account or network is needed for the demo; setting keys does not enable a backend.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
